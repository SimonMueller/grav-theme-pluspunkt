{% extends 'partials/base.html.twig' %}

{% block content %}
  <div class="container">
    <div class="card">
      <div class="content">
        {{ page.content|raw }}
      </div>
    </div>
  </div>
{% endblock %}
