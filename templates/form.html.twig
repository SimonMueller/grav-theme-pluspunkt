{% extends 'partials/base.html.twig' %}

{% block content %}
  <div class="container">
    <div class="card">
      <div class="content">
        {{ page.content }}

        {% include "forms/form.html.twig" %}
      </div>
    </div>
  </div>
{% endblock %}
