{% extends 'partials/base.html.twig' %}

{% block content %}
  <div class="container">
    <div class="card">
      <div class="content">
        <h1>Error {{ header.http_response_code }}</h1>
        {{ page.content }}
      </div>
    </div>
  </div>
{% endblock %}
