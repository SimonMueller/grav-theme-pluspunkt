title: THEME_PLUSPUNKT.ABOUT
extends@: default

form:
  fields:
    tabs:
      type: tabs
      active: 1

      fields:
        advanced:
          fields:
            overrides:
              fields:
                header.child_type:
                  default: person
        about:
          type: tab
          title: THEME_PLUSPUNKT.LIST
          import@:
            type: partials/list
            context: blueprints://
