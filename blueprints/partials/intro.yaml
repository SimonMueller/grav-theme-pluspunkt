form:
  fields:
    header.intro.enabled:
      type: toggle
      label: THEME_PLUSPUNKT.INTRO
      highlight: 0
      default: 0
      options:
        1: PLUGIN_ADMIN.ENABLED
        0: PLUGIN_ADMIN.DISABLED
      validate:
        type: bool

    header.intro.fullscreen:
      type: toggle
      label: THEME_PLUSPUNKT.FULLSCREEN
      highlight: 0
      default: 0
      options:
        1: PLUGIN_ADMIN.ENABLED
        0: PLUGIN_ADMIN.DISABLED
      validate:
        type: bool

    header.intro.text_enabled:
      type: toggle
      label: THEME_PLUSPUNKT.INTRO_TEXT
      highlight: 1
      default: 0
      options:
        1: PLUGIN_ADMIN.ENABLED
        0: PLUGIN_ADMIN.DISABLED
      validate:
        type: bool

    header.intro.text:
      label: THEME_PLUSPUNKT.INTRO_TEXT
      type: textarea
